-- Seed data for local development
-- Note: auth.users must exist first; create test users via signup flow or supabase auth admin

-- Example admin role assignment (replace UUID after creating user):
-- INSERT INTO public.user_roles (user_id, role) VALUES ('<user-uuid>', 'admin');
